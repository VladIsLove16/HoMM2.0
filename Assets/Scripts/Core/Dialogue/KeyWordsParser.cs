using System;
using System.Collections.Generic;
using System.Linq;

internal class KeyWordsParser
{
    private Dictionary<string, string> keys;
    public string Parse(string resolvedText)
    {
       return resolvedText;
    }
}